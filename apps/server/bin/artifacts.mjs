export const serverArtifacts = Object.freeze({
  api: 'api.mjs',
  subagentMcp: 'subagent-mcp.mjs',
  runtime: 'runtime',
  web: 'web',
})

export const serverBundleEntries = Object.freeze(Object.values(serverArtifacts))
